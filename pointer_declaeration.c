// Sachin GAvali 
#include<stdio.h>
#include<conio.h>
int main(){
    int a = 10 ;
    int *ptr = &a;
    printf(" The valve of a is %d\n ",*ptr);// pointer is the store the addrass of anothe variable and use astrick sign to point the value of store in theis addrass 
    printf(" the addrass of a is %p", ptr);//ptr is store the address of a variable 
    return 0; 
}
